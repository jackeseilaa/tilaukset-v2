export function placeholderView(title, subtitle) {
  return `<div class="card">
    <div class="card-title">${title}</div>
    <div class="card-sub">${subtitle}</div>
    <div class="infobox infobox-amber" style="margin-top:14px">🚧 Ei vielä rakennettu — tulossa myöhemmässä vaiheessa suunnitelman mukaisesti.</div>
  </div>`;
}
